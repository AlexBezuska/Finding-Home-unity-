using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanWalkBehind : MonoBehaviour {
  // private Vector3 ThisObjectPosition;


	// Update is called once per frame
	void Update () {
    // ThisObjectPosition = transform.position;
    // Debug.Log(ThisObjectPosition);
    // transform.position = new Vector3 (ThisObjectPosition.x, ThisObjectPosition.y, -ThisObjectPosition.y);

	}
}
